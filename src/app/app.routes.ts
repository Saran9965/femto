import { Routes } from '@angular/router';
import { FrontendComponent } from './frontend/frontend.component';

export const routes: Routes = [
    {path:'form',component:FrontendComponent},
    {path:'',component:FrontendComponent},
];

